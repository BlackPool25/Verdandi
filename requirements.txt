# Pinned runtime deps (CPU-only, no torch). See docs/TECHNICAL.md.
simpy==4.1.2
tigramite==5.2.10.1
networkx==3.6.1
numpy==2.4.6
psutil==7.2.2
scipy==1.18.1
# Offline-validation only (killed as detector, ADR-0009):
stumpy==1.14.1
